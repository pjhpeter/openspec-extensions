interface:
  display_name: "OpenSpec Subagent Team"
  short_description: "Default coordinator entry for complex issue-mode work"
  default_prompt: "Use $openspec-subagent-team as the default coordinator path for this OpenSpec complex change, running development, check, repair, and review rounds through the lifecycle."
